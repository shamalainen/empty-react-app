import React from 'react';

import { Element } from '../..';

import './input.scss';

export const Input = props => <Element TagName="input" {...props} />;

export const Select = props => <Element TagName="select" {...props} />;
